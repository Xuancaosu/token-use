{
  "name": "token-use-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Token Use leaderboard backend",
  "scripts": {
    "start": "node src/server.js",
    "check": "node --check src/server.js"
  },
  "engines": {
    "node": ">=20"
  }
}
